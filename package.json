{
  "name": "faktory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "faktory": "tsx src/cli.ts",
    "test": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration --testTimeout=600000 --hookTimeout=600000",
    "typecheck": "tsc --noEmit",
    "sync-skills": "bash scripts/sync-skills.sh"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.269",
    "commander": "^12.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
